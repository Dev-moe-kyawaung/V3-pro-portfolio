// GitHub remote data source
class GitHubRemoteSource {
  const GitHubRemoteSource();

  Future<Map<String, dynamic>> getUserProfile(String username) async {
    throw UnimplementedError('Connect GitHub API here later.');
  }
}
